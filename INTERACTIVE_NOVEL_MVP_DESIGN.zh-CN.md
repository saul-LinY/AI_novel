# 互动小说 Agent：MVP 怎么做

这份文档只回答一个问题：第一版应该做成什么样，先做哪些功能，哪些事情先不要做。

## 一句话定义

这是一个用户每次做一个动作，系统就继续写一小段剧情的本地网页。

用户可以：

- 自己输入行动，例如“我打开抽屉”。
- 点击系统给出的 2–4 个建议行动。
- 看到行动带来的结果。
- 回到之前的剧情节点，换一种选择继续。

第一版不追求自动写完整本小说，而是先证明：**用户的选择真的会改变故事，而且故事不会很快前后矛盾。**

## 1. 第一版应该包含什么

### 用户能看到的页面

只需要一个故事页面：

1. 上方显示故事标题和当前章节位置。
2. 中间显示正在发生的剧情。
3. 下方有一个输入框，用户可以自由输入行动。
4. 输入框下面显示 2–4 个建议行动。
5. 旁边显示简单的当前状态：地点、时间、重要人物、关键物品。
6. 有一个“查看分支”按钮，可以回到之前的节点。

不要一开始做复杂后台、任务中心、社交、排行榜或多页面工作台。

### 故事规模

先固定一个小世界，不要一上来做开放世界。建议选择：

- 一座旅馆里的悬疑故事；或
- 一个小镇里的调查故事；或
- 一艘船上的封闭冒险故事。

首版只需要：

- 1 个主角。
- 3–5 个重要人物。
- 5–10 个地点。
- 10–20 个关键事实或线索。
- 20–30 个可玩的回合。

封闭场景更容易判断故事是否自洽，也更容易发现问题。

## 2. 每次用户行动后，系统怎么工作

每回合都按照下面的顺序进行：

1. 读取当前故事状态。
2. 读取最近几段正文，以及和当前人物、地点、线索有关的旧信息。
3. 把用户的行动交给 Pi 驱动的模型。
4. 要求模型同时返回四样东西：
   - 这一回合的正文。
   - 2–4 个下一步建议。
   - 这次行动实际改变了什么。
   - 后面必须记住的事情。
5. 应用程序检查“改变了什么”是否合理。
6. 检查通过后，才保存正文和新的故事状态。
7. 网页一边收到正文，一边显示生成过程；保存完成后再显示新的状态。

最重要的一条是：**模型只能提出变化，不能直接修改故事真相。**

例如，模型可以提出“抽屉里拿到一把钥匙”，但程序要检查抽屉是否存在、钥匙是否已经被拿走。如果检查失败，就让模型重写一次，仍然失败就告诉用户这次行动没有成功。

## 3. 只保存几类简单信息

不要先做复杂的知识库。第一版只要保存这些内容：

### 当前状态

- 现在在哪里。
- 故事时间是什么时候。
- 重要人物在哪里、对主角是什么态度、正在追求什么。
- 主角拥有或失去的物品。
- 主角已经知道哪些事情。
- 还没有解决的线索和问题。

### 事件记录

每回合追加一条记录：

- 用户做了什么。
- 系统写了什么。
- 状态发生了哪些变化。
- 这条记录属于哪个分支。

历史记录只追加，不覆盖。这样才能回退、重放和查错。

## 4. 分支怎么做

### 首版规则

- 用户在任意已完成的回合创建新分支。
- 新分支从那个回合的状态开始。
- 新分支之后的行动不会影响旧分支。
- 可以切回旧分支继续。
- 首版不自动合并两个分支。

例如：

```text
第 3 回合：发现一封信
├─ 分支 A：马上拆信
└─ 分支 B：先去找信的主人
```

分支树只需要让用户看懂“我从哪里分出来、现在在哪一条路上”，不用一开始做复杂的剧情图。

## 5. Pi 在这里负责什么

把 Pi 当成“负责和模型沟通的发动机”：

- 负责调用模型。
- 负责把正文逐字传给网页。
- 负责保存对话过程。
- 负责从某个历史节点继续。
- 负责在对话太长时做压缩。

故事状态、分支规则和检查逻辑放在你自己的程序里。不要直接改 Pi 的核心代码，也不要让 Pi 的会话摘要成为唯一记忆。

## 6. 给模型的要求要简单明确

每回合的提示词只需要强调这些规则：

- 只能使用当前状态中已经存在的事实。
- 不要让人物知道他们没有机会知道的秘密。
- 用户的行动可以成功、失败或产生意外，但必须给出明确后果。
- 正文和状态变化分开写。
- 不要擅自结束故事，除非已经满足结局条件。
- 不要改变没有被本回合影响的人物、物品和地点。
- 如果行动违反世界规则，要说明原因，并给出仍然有趣的结果。

模型返回的内容可以按下面的格式组织：

```text
正文：……

下一步：
1. ……
2. ……
3. ……

状态变化：
- 主角获得：……
- 人物关系变化：……
- 新增线索：……

需要记住：
- ……
```

网页只展示正文和下一步；状态变化可以折叠起来，方便调试。

## 7. 必须有的检查

先做规则检查，不要先做“文学质量评分”。至少检查：

- 已经消耗的物品不能再次使用。
- 不在现场的人不能直接参与当前行动。
- 主角没有见过的秘密，不能突然变成“主角已知”。
- 人物已经死亡或离开后，不能无原因继续行动。
- 时间不能倒退，除非用户明确回到旧分支。
- 状态变化必须能在正文中找到原因。
- 每次行动都要成功、失败或改变关系，不能只换一种说法继续原剧情。

如果检查不通过：

1. 先让模型根据错误重新生成一次。
2. 第二次仍不通过，就不保存这一回合，并向用户说明行动没有被接受。

## 8. 推荐的开发顺序

### 第一步：先不用模型

用固定的几条状态变化写一个小测试：拿钥匙、开门、离开房间、回到之前节点。先证明保存、回退和分支隔离没有问题。

### 第二步：接入 Pi

让模型只负责写正文、给建议行动和提出状态变化。网页先用最简单的输入框和正文区域。

### 第三步：加入检查

把上面的物品、地点、秘密、时间和人物规则写成程序检查。发现不合法时重试一次。

### 第四步：加入分支

保存每回合历史和状态版本，允许用户从旧回合继续。先不做分支合并。

### 第五步：再做记忆整理

等 20–30 回合真的能跑通后，再加入“长期线索”“人物目标”“伏笔提醒”等辅助记忆。

## 9. 第一版明确不做什么

- 不做定时自动写作。
- 不做多个 Agent 互相讨论。
- 不做复杂战斗、等级、装备和数值判定。
- 不做自动生成几十条未来分支。
- 不做自动合并分支。
- 不做向量数据库和复杂知识图谱。
- 不做一键生成几十万字长篇。
- 不做通用文件操作和 Shell 工具。

这些功能不是永远不做，而是不能在第一个版本里同时出现，否则很难知道问题到底来自剧情、状态、模型还是界面。

## 10. MVP 是否成功，用这几个问题判断

找 3 个固定故事场景，每个让测试者玩 20–30 回合：

- 选择不同，后面是否真的出现不同结果？
- 回到旧节点后，旧分支是否保持原样？
- 人物、物品、地点和线索是否前后一致？
- 用户输入一个不可能的行动时，系统是否有合理失败，而不是胡编？
- 刷新网页后，是否能继续上次故事？
- 模型失败或网络断开时，是否不会把故事状态写坏？

如果这几个问题都能稳定回答“是”，就说明第一版已经值得继续扩展。

## 最终建议

先把产品做成一个“**小场景、单主角、每回合一个行动、状态可回退**”的互动故事。Pi 只负责模型调用和对话过程，你的程序负责记住世界、判断行动是否合法、保存分支。

第一阶段真正要验证的不是文笔有多华丽，而是：**用户的每一次选择，能不能留下可靠、可回退、会影响后续的结果。**

详细的竞品、论文和 Pi 源码调研仍保留在：[INTERACTIVE_NOVEL_AGENT_RESEARCH.zh-CN.md](INTERACTIVE_NOVEL_AGENT_RESEARCH.zh-CN.md)。

