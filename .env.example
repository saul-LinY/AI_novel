# auto: use Pi when a configured model is available, otherwise use the built-in demo writer
# pi: require a configured Pi model
# demo: never call a remote model
AI_NOVEL_MODE=auto
PORT=4317

# Optional. Defaults to ~/.pi/agent.
# PI_AGENT_DIR=/absolute/path/to/.pi/agent

